export interface SEOHeadProps {
  title: string;
  description: string;
  canonical?: string;
  keywords?: string;
  ogType?: string;
  noIndex?: boolean;
}

const SEOHead = (_props: SEOHeadProps) => null;

export default SEOHead;
